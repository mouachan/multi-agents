{{- if and .Values.minio.enabled .Values.minio.route.console.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "agentic-claims-demo.fullname" . }}-minio-console
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "agentic-claims-demo.labels" . | nindent 4 }}
    app.kubernetes.io/component: minio
spec:
  {{- if .Values.minio.route.console.host }}
  host: {{ .Values.minio.route.console.host }}
  {{- else }}
  host: minio-console-{{ .Values.global.namespace }}.{{ .Values.global.clusterDomain }}
  {{- end }}
  to:
    kind: Service
    name: {{ include "agentic-claims-demo.fullname" . }}-minio
    weight: 100
  port:
    targetPort: console
  {{- if .Values.minio.route.console.tls.enabled }}
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  {{- end }}
  wildcardPolicy: None
{{- end }}
