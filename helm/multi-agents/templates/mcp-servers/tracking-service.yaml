{{- if .Values.mcp.tracking.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-server
  namespace: {{ include "multi-agents.namespace" . }}
  labels:
    {{- include "multi-agents.labels" . | nindent 4 }}
    app: tracking-server
spec:
  type: {{ .Values.mcp.tracking.service.type }}
  ports:
  - port: {{ .Values.mcp.tracking.service.port }}
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: tracking-server
{{- end }}
