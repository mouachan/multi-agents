{{- if and .Values.guardrails.enabled .Values.guardrails.llamaGuard.enabled }}
---
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: guardrails-detector
  namespace: {{ include "multi-agents.namespace" . }}
  labels:
    {{- include "multi-agents.labels" . | nindent 4 }}
    trustyai/detector: "true"  # CRITICAL: Label for GuardrailsOrchestrator auto-discovery
    app: claims-processing
    component: guardrails-detector
  annotations:
    argocd.argoproj.io/sync-wave: "3" # after inference models, before orchestrator
    serving.kserve.io/deploymentMode: RawDeployment
    serving.knative.openshift.io/enablePassthrough: "true"
    sidecar.istio.io/inject: "true"
    sidecar.istio.io/rewriteAppHTTPProbers: "true"
    openshift.io/display-name: "Claims Guardrails Detector (Llama Guard 3 1B)"
spec:
  predictor:
    automountServiceAccountToken: false
    minReplicas: {{ .Values.guardrails.llamaGuard.minReplicas }}
    maxReplicas: {{ .Values.guardrails.llamaGuard.maxReplicas }}
    model:
      modelFormat:
        name: vLLM
      runtime: llama-guard-3-1b
      # Using OCI modelcar from Red Hat AI Services
      storageUri: {{ .Values.guardrails.llamaGuard.storageUri | quote }}
      resources:
        {{- toYaml .Values.guardrails.llamaGuard.resources | nindent 8 }}
{{- with .Values.guardrails.llamaGuard.tolerations }}
    tolerations:
      {{- toYaml . | nindent 6 }}
{{- end }}
{{- with .Values.guardrails.llamaGuard.placement }}
{{ toYaml . | indent 4 }}
{{- end }}
{{- end }}
