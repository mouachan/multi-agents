apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: frontend
  namespace: claims-demo
  labels:
    app: frontend
    component: frontend
spec:
  to:
    kind: Service
    name: frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
