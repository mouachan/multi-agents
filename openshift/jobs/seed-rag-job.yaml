apiVersion: batch/v1
kind: Job
metadata:
  name: seed-rag-data
  namespace: multi-agents
spec:
  template:
    spec:
      containers:
      - name: seed-rag
        image: registry.access.redhat.com/ubi9/python-311:latest
        command:
        - /bin/bash
        - -c
        - |
          pip install --no-cache-dir psycopg2-binary httpx && \
          python3 /scripts/seed_rag_data.py
        env:
        - name: POSTGRES_HOST
          value: "postgresql.multi-agents.svc.cluster.local"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_DB
          value: "claims_db"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgresql-secret
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgresql-secret
              key: POSTGRES_PASSWORD
        - name: EMBEDDING_URL
          value: "https://embedding-service.apps.your-cluster.example.com"
        volumeMounts:
        - name: seed-script
          mountPath: /scripts
      volumes:
      - name: seed-script
        configMap:
          name: seed-rag-script
      restartPolicy: Never
  backoffLimit: 3
