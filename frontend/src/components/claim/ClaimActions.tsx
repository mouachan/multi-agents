/**
 * ClaimActions Component - Process claim button
 * Process button hidden — processing is triggered automatically
 */

import type { Claim } from '../../types'

interface ClaimActionsProps {
  claim?: Claim
  onProcessStart?: () => void
}

export default function ClaimActions(_props: ClaimActionsProps) {
  return null
}
